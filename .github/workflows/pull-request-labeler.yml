name: Auto Label Pull Requests

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

jobs:
  auto-label:
    name: Label PRs Based on File Paths
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: Checkout repo with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0  # Ensures full history for proper diff detection.

      - name: "Debug: List changed files"
        run: |
          git fetch origin ${{ github.base_ref }}
          git diff --name-only origin/${{ github.base_ref }}...HEAD

      - name: Run Labeler
        uses: actions/labeler@v5
        with:
          repo-token: ${{ secrets.API_TOKEN_GITHUB }}
          configuration-path: ${{ github.workspace }}/.github/labeler.yml
          sync-labels: true
